export type UserRequest = {
    FirstName: string,
    LastName: string,
    FullName: string,
    Email: string
}

export type User = {
    Id: string,
    FirstName: string,
    LastName: string,
    FullName: string,
    Email: string
}